# Create a list with duplicate numbers
numbers_list = [1, 2, 2, 3, 4, 4, 4, 5]
print(f"Original list with duplicates: {numbers_list}")

# Convert the list into a set to get unique items
unique_numbers_set = set(numbers_list)
print(f"Set with only unique items:  {unique_numbers_set}") 